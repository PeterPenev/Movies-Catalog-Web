@model ActorViewModel

<!DOCTYPE html>
<html lang="en">
<head>
    <title>@Model.FirstName @Model.LastName details</title>
    <link rel="stylesheet" href="/css/news.css" type="text/css" media="all" />
    <link rel="stylesheet" href="/list-css/list.css" type="text/css" media="all" />
    <link rel="stylesheet" type="text/css" href="/list-css/table-style.css" />
    <link rel="stylesheet" type="text/css" href="/list-css/basictable.css" />
</head>
<body>
    <div class="container">
        <!-- /w3l-medile-movies-grids -->
        <div class="agileits-single-top">
        </div>
        <div class="single-page-agile-info">
            <!-- /movie-browse-agile -->
            <div class="show-top-grids-w3lagile">
                <div class="col-sm-8 single-left">
                    <div class="song">
                        <div class="song-info">
                            <h3>@Model.FirstName @Model.LastName Details</h3>
                        </div>
                        @{ if (Model.Picture == null)
                            {
                                <div class="w3-list-img"><a asp-controller="Actors" asp-action="Details"><img style="border: 5px solid #ff9800" src="~/images/noimage.jpg" alt="" /> <span></span></a></div>
                            }
                            else
                            {
                                <div class="w3-list-img"><a asp-controller="Actors" asp-action="Details"><img style="border: 5px solid #ff9800" src="@Model.Picture" alt="" /> <span></span></a></div>
                            }
                        }
                    </div>
                    <div class="song-grid-right">

                    </div>
                    <div class="clearfix"> </div>

                    <div class="all-comments">
                        <div class="all-comments-info">
                            <a href="#"></a>
                            <div>@Model.Biography</div><br />
                            <div class="agile-info-wthree-box">
                            </div>
                        </div>
                        <div class="media">
                        </div>
                        <div class="media">
                        </div>
                        <div class="agile-info-wthree-box">
                            <form asp-route-returnurl="@ViewData["ReturnUrl"]" method="get">
                                @if (this.User.Identity.IsAuthenticated)
                                {
                                    <input type="submit" asp-action="Update" asp-route-id="@Model.Id" value="Add to movie">
                                    <input type="submit" asp-action="Update" asp-route-id="@Model.Id" value="Update">
                                }
                                <div class="clearfix"> </div>
                            </form>
                        </div>
                    </div>
                </div>
            </div>
            <div class="col-md-4 single-right">
                <h3>Top Rated Movies</h3>
                <ul>
                    @*<li>@await Html.PartialAsync("_TopRatedMoviesPartial")</li>*@
                </ul>
            </div>
        </div>
    </div>
</body>
</html>



